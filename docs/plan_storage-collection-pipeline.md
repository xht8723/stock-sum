# Storage And Collection Pipeline

This historical plan has been superseded by
`docs/plan_remove-x-reddit-playwright-collectors.md`.

The current direction is API-first: keep shared collection run/index storage,
but add source-specific raw tables and mappers only when a future API collector
is implemented.
